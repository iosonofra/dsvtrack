services:
  dsv-tracking-center:
    build: .
    container_name: dsv-tracking-center
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - .env
    volumes:
      - ./data:/app/data
